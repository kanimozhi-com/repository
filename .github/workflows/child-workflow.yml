name: Child Workflow

on:
  workflow_run:
  workflows: ["Parent Workflow"]
  types:
    - completed

jobs:
  child_job:
    runs-on: self-hosted
    steps:
      - run: echo "👋 Hello from the child workflow"
