name: Parent Workflow

on:
  push:
    branches: [main]

jobs: 
  build: 
    runs-on: ubuntu-latest
    steps: 
      - name: checkout code
        uses: actions/checkout@v4

      - name: successful
        run: echo "successfully completed"
